import { Test, TestingModule } from '@nestjs/testing';

// import { LocalStrategy } from './local-strategy';

describe.skip('LocalStrategy', () => {
  it('should be defined', () => {
    // expect(new LocalStrategy()).toBeDefined();
  });
});
